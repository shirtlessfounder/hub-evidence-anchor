[workspace]
members = [
    "programs/hub-evidence-anchor"
]
resolver = "2"

[profile.release]
overflow-checks = true

# Patch transitive deps to avoid Rust 2024 edition requirements
[patch.crates-io]
block-buffer = { version = "=0.11.0" }
