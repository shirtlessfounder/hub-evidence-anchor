[workspace]
members = [
    "programs/hub-evidence-anchor"
]
resolver = "2"

[profile.release]
overflow-checks = true

# Removed: patches were causing cargo resolution errors. Rust 1.85 in CI handles edition 2024 natively.
