[workspace]
members = [
    "programs/hub-evidence-anchor"
]
resolver = "2"

[profile.release]
overflow-checks = true

# Force older transitive deps that require Rust 2024 edition
[patch.crates-io]
toml_edit = { version = "0.21.0" }
blake3 = { version = "1.7.1" }
