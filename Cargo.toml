[workspace]
members = [
    "programs/hub-evidence-anchor"
]
resolver = "2"

[profile.release]
overflow-checks = true

# Pin transitive deps to avoid Rust 2024 edition requirements
[profile.release.package.hybrid-array]
version = "=0.2.0"

[profile.release.package.sha2]
version = "=0.9.0"
