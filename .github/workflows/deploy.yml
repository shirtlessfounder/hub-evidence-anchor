name: Deploy to Solana Devnet

on:
  push:
    branches: [main]
  workflow_dispatch: {}

env:
  PROGRAM_ID: 275QQuz5D6d5U7rhAVW1gYGZBmmyzq6srFdV3rT6rMdA

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Solana CLI
        run: |
          curl -sSfL "https://github.com/anza-xyz/agave/releases/download/v1.18.6/solana-release-x86_64-unknown-linux-gnu.tar.bz2" | tar xj -C /tmp
          echo "/tmp/solana-release/bin" >> $GITHUB_PATH
          /tmp/solana-release/bin/solana --version

      - name: Decode wallet and airdrop
        env:
          WALLET_JSON: ${{ secrets.ANCHOR_WALLET_JSON }}
        run: |
          set -e
          python3 -c "
import os, json, base64

ALPHABET = '123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz'
secret = os.environ.get('WALLET_JSON', '').strip()
print('Secret len:', len(secret), 'start:', secret[:10])

# Try formats in order
# Format: bs58(raw 64-byte keypair) -> big-endian decode
try:
    n = 0
    for c in secret:
        n = n * 58 + ALPHABET.index(c)
    raw = n.to_bytes(64, 'big')
    open('/tmp/keypair.bin', 'wb').write(raw)
    print('Decoded bs58 -> 64 bytes OK')
    pubkey_bytes = raw[32:]
    npub = int.from_bytes(pubkey_bytes, 'big')
    addr = ''
    while npub > 0:
        npub, rem = divmod(npub, 58)
        addr = ALPHABET[rem] + addr
    open('/tmp/pubkey.txt', 'w').write(addr)
    print('Pubkey:', addr)
except Exception as e:
    print('Error:', e)
    exit(1)
"
          PUBKEY=$(cat /tmp/pubkey.txt)
          echo "Airdropping 10 SOL to: $PUBKEY"
          /tmp/solana-release/bin/solana airdrop 10 "$PUBKEY" --url devnet || echo "Airdrop may have rate-limited"

      - name: Deploy pre-built .so
        run: |
          echo "Files:"
          ls -la /tmp/keypair* /tmp/pubkey* 2>/dev/null || echo "No files found"
          echo "Keypair first 3 bytes (hex): $(od -An -tx1 -N3 /tmp/keypair.bin 2>/dev/null || echo 'cannot read')"
          /tmp/solana-release/bin/solana program deploy \
            programs/hub-evidence-anchor/hub_evidence_anchor.so \
            --keypair /tmp/keypair.bin \
            --url devnet \
            --commitment finalized

      - name: Verify deployment
        run: |
          /tmp/solana-release/bin/solana program show $PROGRAM_ID --url devnet
