name: Rebuild and Deploy

on:
  workflow_dispatch:

env:
  PROGRAM_ID: spJAH8mpJmzp6xf5fpfueaBsjRUbPjcmJJMTrfvW8cf
  HOME: /home/runner

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Setup HOME for cargo-build-sbf
        run: |
          mkdir -p $HOME/.cargo/bin
          mkdir -p $HOME/.rustup

      - name: Install Solana CLI + SBF toolchain
        run: |
          curl -sSfL "https://github.com/anza-xyz/agave/releases/download/v1.18.6/solana-release-x86_64-unknown-linux-gnu.tar.bz2" | tar xj -C /tmp
          echo "/tmp/solana-release/bin" >> $GITHUB_PATH

      - name: Link Solana SBF Rust as cargo
        run: |
          # cargo-build-sbf needs cargo at $HOME/.cargo/bin/cargo
          # Solana SBF toolchain has its own cargo at sdk/sbf/cargo
          ln -sf /tmp/solana-release/bin/sdk/sbf/cargo/cargo $HOME/.cargo/bin/cargo
          ln -sf /tmp/solana-release/bin/sdk/sbf/cargo/rustc $HOME/.cargo/bin/rustc
          ln -sf /tmp/solana-release/bin/sdk/sbf/cargo/rustup $HOME/.cargo/bin/rustup 2>/dev/null || true
          echo "cargo version: $($HOME/.cargo/bin/cargo --version)"

      - name: Install Anchor CLI (pre-built, bypass AVM)
        run: |
          npm install -g @coral-xyz/anchor-cli@0.30.0
          # Anchor binary goes to /home/runner/.npm-global/bin/anchor
          echo "$(npm root -g)/bin" >> $GITHUB_PATH
          anchor --version

      - name: Build program
        run: |
          # Use npm-installed anchor (in .npm-global/bin), NOT AVM shim
          # The npm anchor binary calls cargo-build-sbf, which uses Solana SBF Rust
          anchor build --skip-lint

      - name: Decode program wallet
        env:
          PROGRAM_WALLET_JSON: ${{ secrets.ANCHOR_WALLET_JSON }}
        run: |
          python3 << 'PYEOF'
          import os, json, base64
          ALPHABET = "123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz"
          secret = os.environ.get("PROGRAM_WALLET_JSON", "").strip()
          raw = None
          if len(secret) == 88:
              try:
                  n = 0
                  for c in secret:
                      n = n * 58 + ALPHABET.index(c)
                  raw = n.to_bytes(64, "big")
              except ValueError:
                  raw = None
          if raw is None:
              try:
                  raw = base64.b64decode(secret)
                  if len(raw) != 64: raw = None
              except: raw = None
          if raw is None:
              try:
                  arr = json.loads(secret)
                  if isinstance(arr, list) and len(arr) == 64:
                      raw = bytes(int(x) if isinstance(x, int) else x for x in arr)
              except: pass
          if raw is None: print("ERROR"); exit(1)
          with open("/tmp/program-wallet.json", "w") as f: json.dump(list(raw), f)
          npub = int.from_bytes(raw[32:], "big")
          addr = ""
          while npub > 0: npub, rem = divmod(npub, 58); addr = ALPHABET[rem] + addr
          with open("/tmp/program-pubkey.txt", "w") as f: f.write(addr)
          print(f"Wallet: {addr}")
          PYEOF

      - name: Airdrop SOL
        run: |
          PROGRAM_PUBKEY=$(cat /tmp/program-pubkey.txt)
          echo "Balance before:"
          solana balance "$PROGRAM_PUBKEY" --url https://api.devnet.solana.com
          for i in 1 2 3 4 5; do
            if solana airdrop 5 "$PROGRAM_PUBKEY" --url https://api.devnet.solana.com 2>&1 | grep -q "Success"; then
              echo "Airdrop $i succeeded"; break
            fi
            echo "Airdrop $i failed, retry..."
            sleep 5
          done
          echo "Balance after:"
          solana balance "$PROGRAM_PUBKEY" --url https://api.devnet.solana.com

      - name: Deploy
        run: |
          PROGRAM_PUBKEY=$(cat /tmp/program-pubkey.txt)
          solana program deploy \
            programs/hub-evidence-anchor/target/deploy/hub_evidence_anchor.so \
            --keypair /tmp/program-wallet.json \
            --program-id $PROGRAM_ID \
            --url devnet

      - name: Verify
        run: solana program show $PROGRAM_ID --url devnet
